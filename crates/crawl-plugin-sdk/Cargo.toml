[package]
name = "crawl-plugin-sdk"
description = "Guest-side SDK for crawl Cell (plugin) authors"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
wit-bindgen = "0.41"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["rlib"]
